import numpy as np
import caffe

class SegmentationLayer(caffe.Layer):
    def setup(self, bottom, top):
        
    def reshape(self, bottom, top):
    def forward(self, bottom, top):
    def backward(self, bottom, top):
        